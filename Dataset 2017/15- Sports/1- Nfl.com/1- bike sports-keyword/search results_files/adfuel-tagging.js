if (window.adfuelArticle) {
  window.AdFuel.addPageLevelTarget('article', window.adfuelArticle);
}

if (window.adfuelPage) {
  window.AdFuel.addPageLevelTarget('page', window.adfuelPage);
}

if (window.adfuelEvent) {
  window.AdFuel.addPageLevelTarget('event', window.adfuelEvent);
}

if (window.adfuelTopic) {
  window.AdFuel.addPageLevelTarget('topic', window.adfuelTopic);
}

if (window.adfuelPlayer) {
  window.AdFuel.addPageLevelTarget('player', window.adfuelPlayer);
}

if (window.adfuelCategory) {
  window.AdFuel.addPageLevelTarget('category', window.adfuelCategory);
}
